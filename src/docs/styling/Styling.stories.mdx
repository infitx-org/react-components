import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Settings from 'assets/icons/settings.svg';
import Checkbox from 'components/Checkbox';
import Button from 'components/Button';
import Icon from 'components/Icon';
import Row from 'components/Layout/Row';
import Box from "./components/Box";
import "./ColorSchemes/ColorScheme.scss";
import "../../scss/vars.scss";

<Meta title="Styles/How To Guide"/>

# How To Guide for styling with the react-components patterns

Here you can find the rules, color schemes and the icons used to build the components and also what is exported to use in your project.


### Custom Icons

This is a set of custom SVG icons used for specific use cases.

<Row align="space-around">
  <Box size="small">
    <Icon size={30} icon={<Settings />} />
  </Box>
</Row>

### Color Schemes

Adopting the react-components color scheme is easy!

<Row align="space-around">
  <Box size="small" className="color-scheme-blue--regular"/>
  <Box size="small" className="color-scheme-dark-blue--regular"/>
  <Box size="small" className="color-scheme-green--regular"/>
  <Box size="small" className="color-scheme-orange--regular"/>
  <Box size="small" className="color-scheme-red--regular"/>
  <Box size="small" className="color-scheme-gray--regular"/>
  <Box size="small" className="color-scheme-dark-gray--regular"/>
</Row>

These are the base colors available for you to use in your app.


#### Using SCSS

The easiest and most powerful way is to use SCSS.

You can import variables and themes and use them with minimal effort, without worrying about anything else.

Simply import the entrypoint `index.scss` and you are ready to go.

```scss
@import "@modusbox/react-components/lib/index.scss";

.box {
  background: $color__primary;
}
```
<Box size="small" style={{background: `var(--color__primary)`}} />

**Note**: You shouldn't be including the `index.scss` module more than once, as that could lead to duplicated classnames in your bundle.css.

If your target is to adopt a fully modular system, only import the scss modules you need with the variables for the scope.

#### Using CSS

Import the CSS bundle
```tsx
// component file .js, .jsx, .ts, .tsx
import "@modusbox/react-components/lib/index.css";
```
Use the variable in your stylesheet
```css
// component style .css / .scss
.box {
  background: var(--color__primary);
}
```
<Box size="small" style={{background: `var(--color__primary)`}} />


The biggest difference between the 2 options is that if using SCSS you will be able to apply a custom theme by overriding the variables.


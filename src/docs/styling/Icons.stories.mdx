import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Icon from 'components/Icon';
import Row from 'components/Flexbox/Row';
import Box from "docs/components/Box";
import Icons from "docs/components/Icons";

<Meta title="Styles/Icons"/>

Here you can find the rules, color schemes and the icons used to build the components and also what is exported to use in your project.


# Custom Icons

This is a set of custom SVG icons used for specific use cases.

<Row align="space-around" wrap>
  <Icons />
</Row>
